parameters:
  kernel.secret: TESTSecret

framework:
  test: ~
  router:
        resource: "%kernel.project_dir%/src/Resources/config/routing.yaml"

services:
    _defaults:
        autowire: true
        autoconfigure: true

    BernhardWebstudio\PlaceholderBundle\Controller\PlaceholderProviderController:
        autowire: true
